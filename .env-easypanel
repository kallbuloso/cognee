# --- Banco principal (pg + pgvector) ---
DB_USERNAME=cognee
DB_PASSWORD=change_this_strong_password
DB_NAME=cognee_db

# --- Neo4j ---
NEO4J_PASSWORD=change_this_strong_password

# --- Redis ---
REDIS_URL=redis://redis:6379/0

# --- API Keys ---
OPENAI_API_KEY=sk-...         # se for usar OpenAI nos pipelines/mcp
COGNEE_API_KEY=change_me_api   # para proteger a API do Cognee

# --- API (cognee) ---
ENV=production
COGNEE_API_PORT=8000
DB_PROVIDER=postgres
VECTOR_DB_PROVIDER=pgvector
GRAPH_DB_PROVIDER=neo4j

# --- Frontend ---
NEXT_PUBLIC_API_BASE_URL=https://SEU-DOMINIO-API   # usado pelo frontend pra falar com a API

# --- (opcional) Ajustes de memória/timeouts conforme sua VPS ---
